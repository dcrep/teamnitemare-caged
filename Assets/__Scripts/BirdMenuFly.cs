using UnityEngine;

public class BirdMenuFly : MonoBehaviour

    
{
    [SerializeField] AnimationClip clip;
    private float delay = 4f;
    void Start()
    {
        AnimationClip clip;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
